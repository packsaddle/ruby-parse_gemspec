require 'spec_helper'

describe ParseGemspec do
  it 'has a version number' do
    expect(ParseGemspec::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
